function renameProjection(oldName, newName) {
    // Logic to rename the projection
    console.log(`Renaming projection from ${oldName} to ${newName}`);
    
    // You can add your logic here to actually rename the projection
    
    // Return a message or status indicating the success of the renaming operation
    return `Projection renamed successfully from ${oldName} to ${newName}`;
}

// Example Usage
const oldName = "oldProjection";
const newName = "newProjection";
const result = renameProjection(oldName, newName);
console.log(result); // Output: Projection renamed successfully from oldProjection to newProjection
